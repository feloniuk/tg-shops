1. Технологічний стек

Обов’язкове:

Laravel 12, PHP 8.2

MySQL

Redis (кеш + rate limiting + queues)

Blade / простий Vue 3 у деяких місцях (чат)

Викинуто: Kubernetes, Grafana, складний DevOps, S3, ELK, Prometheus, SPA, складні фронтенд-фреймворки, RabbitMQ, S3-backups.

2. Основні модулі (тільки те, що реально потрібно)
✔ Auth

Email + пароль

Google OAuth

Ролі: Admin, Moderator, User

✔ Billing (мінімальний)

Тарифи: Free, Pro, Business

Оплата через Fondy або LiqPay

Webhook для оновлення статусу підписки

Ліміти тарифів (запити/міс, токени/міс)

✔ Асистенти (основний модуль)

CRUD в адмінці

Параметри:

Назва

Опис

System prompt

Temperature

Модель

Тип: чат / одноразовий запит

JSON-відповідь (вкл/викл)

✔ Провайдер LLM (тільки одне потрібне)

Один SDK-провайдер (наприклад OpenAI або TogetherAI)

Зберігання ключа в адмінці (encrypted)

Викинуто:

мультипровайдерність

failover

пріоритезація моделей

окремі provider tables

складні input-schema

3. Використання асистента
✔ Chat UI

Проста сторінка:
/assistants/{slug}

Чат з історією

Streaming (опціонально; якщо не потрібно — прибираємо)

✔ API-доступ

POST /api/v1/assistants/{assistant}/chat

POST /api/v1/assistants/{assistant}/invoke

GET /api/v1/assistants/{slug}/invoke?... (JSON-відповідь)

✔ API-ключі

Один або кілька ключів для користувача

Ліміти згідно тарифу

Викинуто:

Складні scope

Конкурентні ліміти

Кілька provider routes

4. Логування та статистика
✔ Мінімально необхідне логування:

Хто зробив запит

Якому асистенту

Вхідні дані

Вихідні дані

Час, токени

✔ Мінімальна статистика:

Запити за місяць

Топ асистентів

Використані токени

Викинуто:

Audit logs

Advanced analytics

Export

Retention політики

Складні фільтри

5. Адмінка
✔ Обов’язкові екрани:

Dashboard (коротка статистика)

Асистенти (список, створити, відредагувати)

Користувачі (список, ролі, тарифи)

Тарифи (список, ліміти, ціна)

Налаштування LLM (ключ API)

Логи запитів (список, перегляд одного)

Викинуто:

webhooks manager

provider priority

складні builder-и prompt-ів

редактори schema

модуль безпеки з фільтрами

інструменти replay

6. Rate limiting & безпека
✔ Мінімум:

Rate limit: X запитів/хв відповідно до тарифу

Перевірка токенів/міс згідно тарифу

Basic sanitization

Стандартний Laravel security stack

Викинуто:

advanced content filtering

окремий WAF

IP reputation

PII anonymization

7. Arch / DevOps
✔ Мінімальна інфраструктура:

Docker (опціонально)

Один сервер (VPS)

Laravel scheduler + queues

Логи в файли (більше не потрібно)

Викинуто:

Kubernetes

CI/CD pipelines

Моніторинг

S3 storage

Distributed workers

🧱 Фінальна спрощена БД (чиста і достатня)
users

(id, email, password, name, role, plan_id)

plans

(id, name, price, limits_json)

assistants

(id, name, slug, description, config_json, enabled)

assistant_logs

(id, user_id, assistant_id, request_json, response_json, tokens_used, created_at)

api_keys

(id, user_id, key_hash, name, created_at)

subscriptions

(id, user_id, plan_id, status, provider_id, next_billing_date)

🧩 Готовий простий, робочий, стабільний стек фіч
✔ Асистенти
✔ Чат
✔ API
✔ Ліміти
✔ Платежі
✔ Google auth
✔ Логи
✔ Адмінка